using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEditor;

public class NewBehaviourScript
{/*
    [MenuItem("Tools/Assign Floor Tile Material")]
    public static void AssignFloorTileMaterial()
    {
        GameObject[] tiles = GameObject.FindGameObjectsWithTag("ground");
        Material material = Resources.Load<Material>("groundmat");

        foreach (GameObject t in tiles)
        {
            t.GetComponent<Renderer>().material = material;
        }
    }


    [MenuItem("Tools/Assign Wall Tile Material")]
    public static void AssignWallTileMaterial()
    {
        GameObject[] tiles = GameObject.FindGameObjectsWithTag("walls");
        Material material = Resources.Load<Material>("cobble");

        foreach (GameObject t in tiles)
        {
            t.GetComponent<Renderer>().material = material;
        }
    }


     [MenuItem("Tools/Assign Diggable Tile Material")]
    public static void AssignDiggabbleTileMaterial()
    {
        GameObject[] tiles = GameObject.FindGameObjectsWithTag("diggable");
        Material material = Resources.Load<Material>("diggabledirt");

        foreach (GameObject t in tiles)
        {
            t.GetComponent<Renderer>().material = material;
        }
    }*/


}
